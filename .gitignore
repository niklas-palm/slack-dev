node_modules/
cdk.out/
dist/
*.tsbuildinfo
.DS_Store
*.pem
.env
.env.local

# Your agent's own config: channel ids, repo name, agent name. Not secret, but org-specific — copy
# agent.config.example.json to agent.config.json and edit that.
agent.config.json

# Scratch/debug files (review probes, one-off harnesses) — never commit these.
*scratch*
*probe*
redtest*
